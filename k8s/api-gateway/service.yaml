apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: cake-delight

spec:
  selector:
    app: api-gateway

  ports:
    - name: http
      port: 3000
      targetPort: 3000

  type: ClusterIP