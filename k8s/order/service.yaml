apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: cake-delight

spec:
  selector:
    app: order-service

  ports:
    - name: http
      port: 5002
      targetPort: 5002

  type: ClusterIP