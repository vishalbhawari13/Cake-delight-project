apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: cake-delight
spec:
  selector:
    app: catalog-service

  ports:
    - name: http
      port: 5001
      targetPort: 5001

  type: ClusterIP